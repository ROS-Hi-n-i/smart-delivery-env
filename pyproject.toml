[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "smart_delivery_env"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["env"]
